# SVN assets (icone e banner per WP.org — non includere nel ZIP)
svn-assets/

# Design-log method (keel) — never shipped, never committed
design-log/
.plan/

# OS / tooling
.DS_Store
.locks/
.claude-designlog-hooks.log
